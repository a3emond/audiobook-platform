server {
  listen 80;

  location / {
    proxy_pass http://frontend:80;
  }

  location /api/ {
    proxy_pass http://api:3000/api/;
  }

  location /stream/ {
    proxy_pass http://api:3000/stream/;
  }
}